const darkTheme = {
  primaryColor: '#fff',
  secondaryColor: '#eee',
  lightColor: '#282a35',
  backgroundOveralColor: '#282a35',
  backgroungButtonColor: '#fff',
  borderColor: '#c1c1c1'
}

export default darkTheme

export type Theme = {
  primaryColor: string
  secondaryColor: string
  lightColor: string
  backgroundOveralColor: string
  backgroungButtonColor: string
  borderColor: string
}
